#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

/*
Input: nums = [5,2,3,1]
Output: [1,2,3,5]
*/

vector<int> sort_array(vector<int>& nums)
{
    int left = 0;
    int right = nums.size() - 1;
    while(left < right)
    {
        if(left < right)
        {

        }
    }
}

int main()
{
    return 0;
}